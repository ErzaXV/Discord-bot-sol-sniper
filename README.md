This bot is similar to another project I made (c# sniper bot) but this one is made with python and instead of being based on consol app it's based on discord bot but you can also use it as consol based bot if you want.

# this bot requires jupiter & helius api keys
